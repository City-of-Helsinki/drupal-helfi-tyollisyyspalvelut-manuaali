{#
/**
 * @file
 * Default theme implementation to present a contact info entity.
 *
 * This template is used when viewing a registered contact info's page,
 * e.g., /admin/content/contact-info)/123. 123 being the contact info's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_contact_info()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
