{% set layout_modifiers = [] %}

{% set main_base_class = main_base_class|default('main') %}
{% set main_modifiers = [] %}

{% set main_content_base_class = page_base_class|default('main-content') %}
{% set main_content_modifiers = [] %}
{% set main_content_id = main_content_id|default('main-content-id') %}

  <div {{ bem('content-column', content_colum_modifiers, content_column_base_class, ['']) }}>
    {% block page_content %}
      {% if content.field_hide_header[0]['#markup'] == 0 or content.field_hide_header[0]['#markup'] is empty %}
        {% include "@atoms/text/headings/_heading.twig" with {
              heading_level: '1',
              heading: node__label,
              heading_base_class: 'page-heading',
            }
          %}
      {% endif %}
      {% endblock %}
      {% block content %}
        {{ content|without('field_other_services','field_hide_header') }}
      {% endblock %}
  </div>

{{ content.field_other_services }}
